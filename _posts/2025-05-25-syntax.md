## Talking about code

If it's just a field, like `[Sales]`, it's simple enough. 

And a block is not so bad. 
```sql
sum(
  if [Observation] = 'Yes'
  then [Point Value]
  else 0
  end
)
```

There's no easy way to highlight parameters, but this is a good start. 